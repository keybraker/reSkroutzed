/**
 * External catalogue the price checker compares the Skroutz offer against.
 * Only one provider is shown at a time, keeping the comparison row two-column.
 */
export enum PriceProvider {
  BEST_PRICE = 'BEST_PRICE',
  SHOPFLIX = 'SHOPFLIX',
}
